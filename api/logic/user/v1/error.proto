syntax = "proto3";

package logic.user.v1;

option go_package = "rockim/api/logic/user/v1;v1";

enum ErrorReason {
  UN_SPECIFIED = 0; // 未知错误
  USER_NOT_FOUND = 1; // 用户不存在
  ACCOUNT_REGISTERED = 2; // 账号已注册
}
