syntax = "proto3";

package logic.user.v1;

option go_package = "rockim/api/logic/platform/v1;v1";

enum ErrorReason {
  UN_SPECIFIED = 0; // 未知错误
  TENANT_NOT_FOUND = 1; // 租户不存在
  APP_NOT_FOUND = 2; // 应用不存在
}
