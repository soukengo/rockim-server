server:
  grpc:
    addr: :6103
    timeout: "10s"

database:
  mongodb:
    key: default
cache:
  default:
    expire: "24h"
  categories:
    - category: "product"
      expire: "24h"
  redis:
    key: default


log:
  level: info
  appender:
    fileName: ./log/rockim/group/app.log

