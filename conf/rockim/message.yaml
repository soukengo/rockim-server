server:
  grpc:
    addr: :6105
    timeout: "10s"
  job:
    queue:
      delayed:
        redis:
          key: default
          consumer:
            worker: 10

database:
  mongodb:
    key: default

cache:
  default:
    expire: "72h"
  categories:
  redis:
    key: default

queue:
  general:
    kafka:
      key: default
  delay:
    redis:
      key: default
lock:
  redis:
    key: default