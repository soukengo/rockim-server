server:
  grpc:
    addr: :6102
    timeout: "10s"

cache:
  default:
    expire: "72h"
  categories:
  redis:
    key: default

database:
  mongodb:
    key: default

log:
  level: info
  appender:
    fileName: ./logs/rockim/platform/app.log