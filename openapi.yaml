# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: UserAPI API
    description: 用户接口定义
    version: 0.0.1
paths:
    /api/v1/user/register:
        post:
            tags:
                - UserAPI
            operationId: UserAPI_Register
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/rockim.csdk.v1.UserRegisterRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/rockim.csdk.v1.UserRegisterResponse'
components:
    schemas:
        rockim.csdk.v1.UserRegisterRequest:
            type: object
            properties:
                appId:
                    type: string
                    description: 所属应用
                bucket:
                    type: string
                    description: 用户所属分桶（可选）
                account:
                    type: string
                    description: 用户账户名，由接入方指定
                name:
                    type: string
                    description: 用户名称
                fields:
                    type: object
                    additionalProperties:
                        type: string
                    description: 用户自定义字段
            description: UserRegisterRequest 注册用户请求
        rockim.csdk.v1.UserRegisterResponse:
            type: object
            properties:
                user:
                    $ref: '#/components/schemas/rockim.csdk.v1.types.User'
            description: UserRegisterResponse 注册用户响应
        rockim.csdk.v1.types.User:
            type: object
            properties:
                id:
                    type: string
                    description: 用户ID
                createTime:
                    type: integer
                    description: 创建时间
                    format: int64
                appId:
                    type: string
                    description: 所属应用
                bucket:
                    type: string
                    description: 用户所属分桶（可选）
                account:
                    type: string
                    description: 用户账户名，由接入方指定
                name:
                    type: string
                    description: 用户名称
                status:
                    type: integer
                    description: 状态
                    format: enum
                fields:
                    type: object
                    additionalProperties:
                        type: string
                    description: 客户自定义字段
            description: 用户
tags:
    - name: UserAPI
